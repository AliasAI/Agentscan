# 应用配置
APP_NAME=8004scan API
APP_VERSION=1.0.0
DEBUG=true

# 服务器配置
HOST=0.0.0.0
PORT=8000

# 数据库配置
# SQLite (开发环境)
DATABASE_URL=sqlite:///./8004scan.db

# PostgreSQL (生产环境)
# DATABASE_URL=postgresql://user:password@localhost:5432/8004scan

# CORS 配置
CORS_ORIGINS=["http://localhost:3000", "http://127.0.0.1:3000"]

# 区块链配置
# Sepolia RPC URL (获取方式: https://www.quicknode.com/ 或 https://infura.io/)
SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/YOUR_PROJECT_ID

# AI 分类配置（可选）
# 用于自动分类 agent 的 skills 和 domains
# 如果不配置，系统会使用基于关键词的简单分类
# 获取 API Key: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-YOUR_API_KEY_HERE
